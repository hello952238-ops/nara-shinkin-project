# PROJECT RULES

## CSS構成

- common.css

- ページ専用CSS

ページ専用CSS同士は読み込まない。

---

## HTML

既存ページを優先して流用する。

---

## CSS

common.cssは基本変更しない。

ページ固有CSSには

- ページ固有レイアウト

- ページ固有装飾

のみを書く。

---

## レスポンシブ

ブレークポイント

960px / 961px

SPでは、キャプションなど明らかに小さくてよい文字を除き、本文を含む通常テキストの最小サイズを以下とする。

```css
font-size: clamp(calc(3.6rem * 0.375), 3.75vw, 3.6rem);
```

360px幅で本文が12px相当になる指定は使用しない。

必ず

- PC

- 961px

- 960px

- 390px

- 375px

で確認する。

---

## 共通化

実装中はcommon.cssを変更しない。

候補だけ報告する。

---

## 実装

実装前

- 画像素材確認

- 既存HTML確認

- 不足素材確認

実装後

レビューを実施する。
